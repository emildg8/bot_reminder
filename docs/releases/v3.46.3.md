# v3.46.3 — группы: polish assignee + routing

**Дата:** 2026-06-03 · **Предыдущий:** [v3.46.2.md](v3.46.2.md)

## Исправления

- Срабатывание: `⏰ Emil, тест` вместо `⏰ участник, тест` (first_name fallback)
- `bot_mention` UTF-16 — emoji перед @бот
- `/ping` в группе показывает Group Privacy

## Улучшения

- Общий фильтр `USER_PHRASE_TEXT` (tips/create)
- Единый display name в `format_reminder_message`

## Проверка

- **595** тестов · `verify_ops` OK

```bash
pytest tests/test_stars_tips.py tests/test_telegram_format.py tests/test_bot_mention.py -q
python scripts/smoke_group_mentions.py
```

Telegram smoke:

1. `/ping` → v3.46.3 + `Group Privacy: выкл`
2. `@break_remind_bot` + Emil из @ + `через 1 мин тест` → reply `👌 👤 Emil · …`
3. Confirm → срабатывание с **именем**, не «участник»
