# v3.5.4 — надёжный старт бота

### Исправлено
- Перед polling сбрасывается webhook (если был — бот молчал)
- В лог пишется `@username` — проще проверить, тот ли это бот
- `start.sh` падает сразу, если `BOT_TOKEN` пустой
