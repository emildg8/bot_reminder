# v3.31.0 — menu help/about and search E2E tests

## Тесты
- `menu:help` — справка для лички
- `menu:about` — версия из `__version__`
- `send_search_results` — «Найдено: N» и «ничего не найдено»
- `tests/callback_helpers.make_message` — мок Message для handler-тестов

## CI
- Coverage gate **52%** (факт ~54%)
