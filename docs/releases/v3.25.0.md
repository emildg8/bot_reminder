# v3.25.0 — Handler tests (callbacks)

## Тесты
- `tests/test_001_callbacks_handlers.py` — 7 сценариев с моком Bot/CallbackQuery
- `tests/db_helpers.py` — shared in-memory DB для handler-тестов

## Покрытые handlers
- `confirm:` → create + schedule
- duplicate + `confirm_force:`
- `confirm_edit:` → apply_parsed
- `cancel:` → discard draft
- `done:` → deactivate
