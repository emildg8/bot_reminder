# v3.45.2 — 10/10 quality gate

**Дата:** 2026-06-02

## Новое

- Тесты callback assignee (`as:0`, `as:_none`, `as:_cancel`, истёкший pending)
- Handler: `@бот @a @b` без времени → кнопки, не confirm
- `verify_ops` + smoke NLP / group-mentions / Stars (офлайн, в CI)

## Проверка

```bash
python scripts/verify_ops.py
pytest tests/test_assignee_callbacks.py tests/test_006_collective_handlers.py -q
```
