# v3.46.2 — группы: fix `@бот` routing

**Дата:** 2026-06-03

## Исправления

- `tips.py` перехватывал все текстовые сообщения раньше `create.py` и молча выходил — `@бот Emil …` не доходил до парсера
- Фильтр `tip_custom_text_filter`: handler Stars только в режиме custom amount
- Напоминание из режима Stars → делегирование в `_handle_collective_phrase_message`

## Проверка

- **592** теста · `verify_ops` OK

```bash
pytest tests/test_stars_tips.py tests/test_006_collective_handlers.py -q
```

Telegram smoke: `@break_remind_bot` + Emil из @ + `через 1 мин тест` → reply `👌 👤 …`
