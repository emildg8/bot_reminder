# v3.46.4 — assignee UX + user smoke

## Изменения

- Нерешённое display name + `@user` в фразе без времени → кнопки выбора assignee
- Офлайн-smoke «как пользователь»: `python scripts/smoke_user_group_assignee.py`
- `verify_ops` / `make smoke-user-group`

## Проверка

```bash
python scripts/verify_ops.py
python scripts/smoke_user_group_assignee.py
pytest tests/test_assignee_prompt.py -q
```

## Prod smoke (ручной)

1. `/ping` → v3.46.4
2. `@бот` + тап **Emil** из @ + `через 1 мин тест`
3. Срабатывание → `⏰ Emil, тест`
