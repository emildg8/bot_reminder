# v3.32.0 — menu examples/status, handler tests complete

## Тесты
- `menu:examples` — список примеров с `ex:*`
- `ex:5` → confirm-карточка «таблетки»
- `ex:999` → alert «не найден»
- `menu:status` — активных: 1, версия
- `menu:timezone` — выбор часового пояса

## Инфраструктура
- `make_bot()` в callback_helpers
- patched_db: `create`, `chat_status`

## CI
- Coverage gate **55%** (~56%)
- Handler callback roadmap закрыт (confirm → snooze → menu/list/search)
