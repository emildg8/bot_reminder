# v3.44.7 — группы: варианты assignee + clean punctuation

**Дата:** 2026-06-02

## Парсинг assignee

- `extract_username_anywhere(..., pick="first" | "last")` — выбор первого/последнего `@user` при нескольких упоминаниях.
- `extract_username_anywhere(..., pick="nearest_time")` — выбор `@user`, ближайшего к тайм-фразе.
- `extract_username_candidates()` — возврат всех кандидатов (`@user`) + clean без mention для будущей A/B-логики.
- Сообщения вида `текст ... @бот @user ...` учитывают только часть после первого `@бот` при выборе assignee.

## UX clean-текста

- После удаления mention больше нет артефактов вида `тест !`; теперь нормализуется до `тест!`.
- Поддержаны хвостовые знаки после mention: `@user!`, `@user?`, `@user,`.
- Убираются пустые скобки после удаления mention: `(@user)` → без `()`.

## Проверка

```bash
ruff check bot tests scripts
python scripts/verify_ops.py
pytest tests/test_mention_parse.py tests/test_mention_from_message.py tests/test_mention_create.py -q
python scripts/smoke_group_mentions.py
```
