# v3.37.0 — Infra: Alembic, PostgreSQL, Groq STT

## Alembic
- `alembic/` + initial migration `20260531_0001`
- `bot/db/migrate.py` — upgrade + stamp для существующих SQLite
- `scripts/migrate_db.py`, `make migrate`
- PRAGMA migrations удалены из `init_db`

## PostgreSQL
- `postgresql+asyncpg://` в DATABASE_URL
- `docker compose --profile postgres up -d`
- deps: asyncpg, psycopg2-binary (Alembic sync)

## STT
- `LOCAL_WHISPER_ENABLED=false` — только Groq (+ Yandex опционально)
- Меньше RAM на Wispbyte, ffmpeg не обязателен с Groq

## verify_ops
- Проверка alembic, migrate.py, docker-compose, LOCAL_WHISPER default
