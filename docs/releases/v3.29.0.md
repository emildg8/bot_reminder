# v3.29.0 — menu/list handler tests

## Тесты
- `tests/test_003_callbacks_menu_list.py` — 7 сценариев
- `menu:list` — answer с активными / пустым списком
- `menu:home` — inline-меню
- `list:page` — edit + пагинация (9 напоминаний)
- `list:tab:history` — пустая история
- `list:noop` — callback answer

## Инфраструктура
- `patched_db` патчит async_session в menu/reminders_ui/search_ui
- `make_callback` — `message.from_user` + `chat.type`

## CI
- Coverage gate **47%** (факт ~53%)
