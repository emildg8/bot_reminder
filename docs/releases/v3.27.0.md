# v3.27.0 — del_cancel и snooze ± handler tests

## Тесты
- `del_cancel:` — «Отменено.» + `menu:list`, напоминание остаётся активным
- `sz+:` / `sz-:` — изменение минут в picker, ответ с форматированным временем
- clamp — не ниже 5 мин при многократном «−»

## CI
- Coverage gate **42%** (факт ~47%)
