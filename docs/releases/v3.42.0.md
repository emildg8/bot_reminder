# v3.42.0 — Stars, audit DB, broadcast draft

**Дата:** 2026-06-02

## Продукт (волна H)

### H1 — Telegram Stars / Pro
- Оплата Pro через Stars (`STARS_PAYMENTS_ENABLED`, `PRO_STARS_PRICE`, `PRO_DURATION_DAYS`)
- `/subscribe` + кнопка «⭐ Купить Pro»; `pre_checkout` / `successful_payment`
- Таблица `star_payments`, `users.pro_expires_at`, авто-expire каждые 6 ч

### H3 — Admin audit в БД
- Таблица `admin_actions`; `/adminlog` переживает рестарт

### H4 — Broadcast черновик в БД
- Таблица `broadcast_drafts`; превью рассылки не теряется при рестарте

### H2 — PostgreSQL backup
- Гайд [postgres-backup.md](../guides/postgres-backup.md), cron-пример
- `scripts/pg_backup.sh` (из v3.41)

## Качество (волна I)

- Тесты reminder_history, scheduler, Stars, admin smoke
- pre-commit: `verify_ops`
- Карта admin-тестов в quality-metrics

## Миграция

```bash
alembic upgrade head   # 20260602_0003
```

## Включение Stars (prod)

```env
MONETIZATION_ENABLED=true
STARS_PAYMENTS_ENABLED=true
PRO_STARS_PRICE=250
PRO_DURATION_DAYS=30
```

BotFather → Payments → Telegram Stars.

## Ops (волна J)

См. [ops-checklist.md](../guides/ops-checklist.md) — secrets, smoke, `/ping` v3.42.
