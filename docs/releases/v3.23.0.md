# v3.23.0 — Уточнение времени и режимы

## UX
- `/cancel`, `/menu`, `/help` — сбрасывают pending-уточнение (кнопки времени)
- Поиск — тоже сбрасывает pending
- Редактирование — после «завтра в 2» режим edit не теряется до выбора кнопки

## NLP
- **«напомни завтра в 2 созвон»** — префикс «напомни» не мешает
- **«завтра в два созвон»** — час словами (голос/STT)

## Refactor
- `bot/services/ambiguous_prompt.py` — общая логика create/edit
