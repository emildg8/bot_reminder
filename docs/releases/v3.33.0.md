# v3.33.0 — Stability, smoke tests, product audit

## Bugfix (критично)
- `bot/main.py` — импорт `write_heartbeat`, `exit_for_restart` (NameError при старте)
- `bot/handlers/admin.py` — импорт `format_uptime`, `uptime_seconds` (`/sysinfo`)

## Тесты
- `tests/test_smoke_imports.py` — wiring, который pytest-cov не ловит
- `tests/test_004_edit_settings_handlers.py` — edit + settings callbacks

## Документация
- `docs/plans/product-audit-2026.md` — оценка идеи/реализации, план до v1.0, оптимизация времени

## CI
- Coverage gate **55%** (без иск искусственного bump — batch releases)
