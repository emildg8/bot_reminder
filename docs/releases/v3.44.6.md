# v3.44.6 — группы: парсинг polish

**Дата:** 2026-06-02

## Парсинг

- `@user,фраза` и разделители `+`, `—`, `:` без обязательного пробела
- Все `@бот` убираются из текста напоминания; лишние пробелы схлопываются
- Голос: `@user` в конце транскрипции после reply на сообщение

## UX

- Welcome / подсказки в группе: примеры `@бот@user`, `@бот + @user`

## Проверка

```bash
make smoke-group-mentions
pytest tests/test_mention_parse.py tests/test_mention_create.py -q
```
