# v3.40.2 — CI и Docker fix

**Дата:** 2026-06-02

## Fix

- `test_admin_panel_opens` — in-memory БД (`patched_db`), CI без каталога `data/`
- Тесты `test_admin_panel_service.py` — покрытие admin_panel, gate 65%
- Dockerfile: `alembic/` + `alembic.ini` (миграции в контейнере)
- docs: 401 тест в group-assignee.md

## Деплой

Без изменений схемы после v3.40.0. При первом деплое v3.40.x:

```bash
alembic upgrade head
```
