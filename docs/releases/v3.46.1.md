# v3.46.1 — группы: polish display name preview

**Дата:** 2026-06-03

## Исправления

- Group preview: `👤 Emil · …` вместо `👤 @Emil` для имён из списка
- Unresolved plain name: `👤 Emil?` (не `@Emil?`)
- `_looks_like_telegram_username()` — display name vs @username
- Подсказки gmenu / «Как создать» — тап по имени в @
- Duplicate confirm — `reply_to_message_id` в группе

## Проверка

- **589** тестов · `verify_ops` OK

```bash
pytest tests/test_collective_preview.py tests/test_assignee_pick_note.py -q
python scripts/smoke_group_mentions.py
```
