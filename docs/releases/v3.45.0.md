# v3.45.0 — assignee: выбор кнопками

**Дата:** 2026-06-02

## Новое

- Несколько `@user` **без времени** → кнопки «Кому напомнить?» (`@alice` / `@bobby` / «Только мне»)
- Модуль `create_confirm` — единая отправка confirm (create, edit, выбор assignee)
- Ответ на сообщение бота в группе; превью `@user?` если участник не найден

## Проверка

```bash
pytest tests/test_assignee_prompt.py tests/test_006_collective_handlers.py -q
python scripts/smoke_group_mentions.py
```
