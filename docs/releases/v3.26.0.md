# v3.26.0 — Snooze/delete handler tests

## Тесты
- `tests/test_002_callbacks_snooze_delete.py` — 6 сценариев delete/snooze/access
- `tests/callback_helpers.py` — общие `make_callback`, `once_parsed`, `patch_scheduler`

## Покрытые handlers
- `delete:` / `del_confirm:` — удаление с подтверждением
- `sza:` / `szm:` — отложить и меню snooze
- access control — `delete`/`done` для чужого user_id

## CI
- Coverage gate поднят до **40%**
