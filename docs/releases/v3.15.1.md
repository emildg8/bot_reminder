# v3.15.1

## Встроенный админ
- Telegram id **292396648** всегда в списке админов (merge с `ADMIN_TELEGRAM_IDS` из .env)

## Fix: crash при auto-update на старте
- Раньше: обновление до `start_polling()` → `stop_polling()` → `RuntimeError: Polling is not started`
- Теперь: на старте после `git pull` — чистый shutdown и `execv` без stop_polling
