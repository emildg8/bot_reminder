# v3.44.4 — Stars passthrough + deploy check

**Дата:** 2026-06-04

- «abc» / короткий мусор в «своя сумма» → ошибка, режим не сбрасывается
- «завтра созвон» / «завтра» → passthrough в напоминания
- Админам при старте: `✅ Бот запущен · v3.44.4 · <sha>`
- `scripts/check_deploy.py` — GitHub main + опционально getMe
- Группы: `@бот@user ...` и `@бот ...` корректно нормализуются перед NLP-парсером
- Группы: `@бот + @user ...` теперь тоже корректно разбирается
- Группы: `@user` в середине/конце фразы (`... @user`) тоже назначается корректно
