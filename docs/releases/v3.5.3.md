# v3.5.3 — починка деплоя и групп

### Исправлено
- CI снова проходит — **деплой на Wispbyte** больше не пропускается из-за lint
- В группах: логи, ответ при ошибке, без лишней reply-клавиатуры

### Если бот молчит
1. Напиши в группе: `/ping@break_remind_bot`
2. Если нет ответа — **перезапусти сервер** на Wispbyte (после отключения света)
